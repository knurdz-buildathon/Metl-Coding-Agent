[project]
name = "metl-agent"
version = "0.1.0"
description = "Autonomous cloud coding agent using Aider, browser-use, and LangGraph"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "langgraph>=0.4.0",
    "langchain-openai>=0.3.0",
    "langchain-anthropic>=0.3.0",
    "langchain-google-genai>=2.0.0",
    "aider-chat>=0.86.0",
    "browser-use>=0.11.0",
    "playwright>=1.50.0",
    "redis>=5.2.0",
    "httpx>=0.28.0",
    "pydantic>=2.10.0",
    "pydantic-settings>=2.7.0",
    "python-dotenv>=1.0.0",
    "websockets>=14.0",
    "sse-starlette>=2.2.0",
    "aiosqlite>=0.20.0",
    "GitPython>=3.1.44",
    "rich>=13.9.0",
    "litellm>=1.60.0",
]

# Optional dependencies are installed explicitly in Dockerfile
# v0-sdk and test tools are not in main dependencies to avoid build issues

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]